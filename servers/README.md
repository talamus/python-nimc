# Server Definitions

Create your server definition directories here:

```bash
mkdir some-server
cp ../server_skeleton/server.toml some-server/
```

Location of this directory can be set by `servers_path` in `.env`.
